---
- name: Deploy Cue
  hosts: '*'
  become: true
  become_user: root

  tasks:

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true

    - name: Install required packages
      ansible.builtin.apt:
        state: present
        update_cache: true
        name:
          - ca-certificates
          - curl
          - gnupg2
          - apt-transport-https
          - python3-docker
          - cec-utils
          - iptables

    - name: Create directory for Docker GPG key
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'

    - name: Add Docker GPG key
      ansible.builtin.get_url:
        url: https://download.docker.com/linux/ubuntu/gpg
        dest: /etc/apt/keyrings/docker.asc
        mode: '0644'

    - name: Add Docker APT repository
      ansible.builtin.apt_repository:
        repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian {{ ansible_distribution_release }} stable"
        state: present


    - name: Add Docker repository
      ansible.builtin.apt_repository:
        repo: deb [arch=arm64] https://download.docker.com/linux/debian {{ ansible_distribution_release }} stable
        state: present

    - name: Install Docker packages
      ansible.builtin.apt:
        state: present
        update_cache: true
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin

    - name: Start and enable Docker service
      ansible.builtin.service:
        name: docker
        state: started
        enabled: true

    - name: Add user pi to docker group
      ansible.builtin.user:
        name: 'pi'
        groups: docker
        append: true

    # --- Firewall ---
    - name: Install netfilter-persistent
      ansible.builtin.apt:
        state: present
        name:
          - netfilter-persistent
          - iptables-persistent

    - name: Allow port 5000 from Semaphore server only
      ansible.builtin.iptables:
        chain: INPUT
        protocol: tcp
        destination_port: "{{ api_port | default(5000) }}"
        source: "{{ semaphore_ip }}"
        jump: ACCEPT

    - name: Block port 5000 from all other sources
      ansible.builtin.iptables:
        chain: INPUT
        protocol: tcp
        destination_port: "{{ api_port | default(5000) }}"
        jump: DROP

    - name: Save iptables rules
      ansible.builtin.command:
        cmd: netfilter-persistent save
      changed_when: true

    # --- Project directory ---
    - name: Create directory for Cue
      ansible.builtin.file:
        path: /home/pi/cue
        state: directory
        owner: pi
        group: pi
        mode: '0755'

    - name: Generate config.yml from template
      ansible.builtin.template:
        src: config.yml.j2
        dest: /home/pi/cue/config.yml
        owner: pi
        group: pi
        mode: '0600'

    - name: Copy docker-compose.yml
      ansible.builtin.copy:
        src: ./docker-compose.yml
        dest: /home/pi/cue/docker-compose.yml
        mode: '0644'

    - name: Pull latest container image
      ansible.builtin.command:
        cmd: docker compose pull
        chdir: /home/pi/cue/
      changed_when: true

    - name: Run the container
      ansible.builtin.command:
        cmd: docker compose up -d --force-recreate
        chdir: /home/pi/cue/
      changed_when: true

    - name: Ensure container is running
      community.docker.docker_container:
        name: cue
        state: started
        restart_policy: unless-stopped

    - name: Install fbi for framebuffer image display
      ansible.builtin.apt:
        state: present
        name: fbi

    - name: Check if splash image exists locally
      ansible.builtin.stat:
        path: "{{ playbook_dir }}/splash/splash.png"
      register: splash_image
      delegate_to: localhost
      become: false

    - name: Copy splash image
      ansible.builtin.copy:
        src: ./splash/splash.png
        dest: /home/pi/cue/splash.png
        owner: pi
        group: pi
        mode: '0644'
      when: splash_image.stat.exists

    - name: Create splash screen systemd service
      ansible.builtin.copy:
        dest: /etc/systemd/system/cue-splash.service
        mode: '0644'
        content: |
          [Unit]
          Description=Cue Splash Screen
          After=multi-user.target

          [Service]
          Type=simple
          ExecStartPre=/bin/sh -c 'echo -ne "\033[?25l" > /dev/tty1'
          ExecStart=/usr/bin/fbi -T 1 -noverbose -a /home/pi/cue/splash.png
          StandardInput=tty
          TTYPath=/dev/tty1
          TTYReset=yes
          TTYVHangup=yes
          Restart=always
          RestartSec=5

          [Install]
          WantedBy=multi-user.target
      notify: Reload systemd

    - name: Enable and start splash service
      ansible.builtin.systemd:
        name: cue-splash
        enabled: true
        state: "{{ 'started' if splash_image.stat.exists else 'stopped' }}"
        daemon_reload: true

  handlers:
    - name: Reload systemd
      ansible.builtin.systemd:
        daemon_reload: true
